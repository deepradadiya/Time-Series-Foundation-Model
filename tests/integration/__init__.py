"""Integration tests for end-to-end pipelines.

This sub-package contains integration tests that verify complete pipeline stages
work together correctly: data pipeline, training loop, inference pipeline,
evaluation pipeline, and Gradio app functionality using small synthetic data.
"""
