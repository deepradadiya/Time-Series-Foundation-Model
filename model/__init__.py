"""PatchTST model architecture package.

This package contains the transformer-based model components: patch embedding with
positional encoding, multi-head self-attention, transformer encoder layers, the full
encoder stack, and the top-level PatchTST model assembly. It is used by the
pretraining and forecasting packages for training and inference.
"""
